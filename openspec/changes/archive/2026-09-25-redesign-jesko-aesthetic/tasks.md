# Tasks

## 1. Reset to baseline (discard prior change)

- [x] 1.1 Run `git status` and confirm the only uncommitted work is the `add-scroll-animations` change (modified `index.html` + 4 docs, untracked `images/`, `docs/site/reviews/review-add-scroll-animations.md`, `openspec/changes/add-scroll-animations/`); verify nothing else would be lost — confirmed via `git status` + `git diff --stat HEAD`
- [x] 1.2 `git restore` the modified tracked files back to `9de3ff3` and remove the untracked add-scroll-animations artifacts (`images/`, that review file, `openspec/changes/add-scroll-animations/`) — leaving `openspec/changes/redesign-jesko-aesthetic/` (untracked) intact; verify `git status` shows a clean tree apart from this change folder and `git diff 9de3ff3 -- index.html` is empty — done, both verifications pass
- [x] 1.3 Confirm the planner still works from the baseline in the browser pane (`python3 -m http.server 8123`) before any reskin — one quick smoke check (place an item, see metrics update) — verified: placing items updates metrics (7/80 sq ft), catalog shows icon/color swatches (no images), confirming clean baseline

## 2. Design foundation (type, palette, scaffolding)

- [x] 2.1 Add a lightweight display grotesque via a Google Fonts `<link>` in `<head>`; wire it into `tailwind.config` as a `display` font family; verify it loads (network 200) and applies to a test heading — Space Grotesk added; `font-display`/`display-xl`/`display-lg` classes apply across all headings (verified visually)
- [x] 2.2 Define the section palette journey as CSS custom properties / Tailwind tokens (deep-espresso hero → warm cream → cool "blueprint" → near-black finale), grounded in the existing `wood`/`stone` scale; verify each token resolves to the intended color in the browser — added `espresso` tokens; journey runs espresso hero → cream manifesto/advantages → gold showcase → light workbench → dark blueprint → near-black finale (verified via screenshots)
- [x] 2.3 Establish the full-bleed section scaffold (each marketing section owns its background color/image and text color) and the fixed minimal top nav; verify sections stack full-width with no horizontal overflow at desktop and 375px — fixed mix-blend nav + full-bleed sections; no horizontal overflow at desktop or 375px (verified)

## 3. Cinematic hero (portal + scroll zoom-through)

- [x] 3.1 Build the full-bleed hero on the portal metaphor (looking through a doorway/window into a designed studio space), with oversized lightweight display headline; verify it fills the viewport and is legible over the image — dark portal (hero-workspace.jpg) with "We shape / space" flanking display headline; verified legible (screenshot)
- [x] 3.2 Add the scroll-linked zoom-through as **progressive enhancement**: `animation-timeline: scroll()` scaling the portal layer into the next scene, over a legible static/opacity baseline; verify in-browser whether the transform actually applies (`getComputedStyle`), and if it does not, ship the opacity/cross-fade fallback and record it (design.md Decision 4 / Risks) — **CSS `scroll()` transform confirmed inert in this engine** (portal stayed scale(1) mid-scroll, as the prior change found); replaced with a JS rAF-throttled scroll handler (`initHeroZoom`) that scales+fades the portal — verified working (scale 1.98 / opacity 0.45 at scrollY 306). Baseline is fully legible at scale(1) if JS never runs.
- [x] 3.3 Add the persistent floating pill CTA ("Start Planning") pinned bottom-center that smooth-scrolls to the planner; verify it stays fixed across scroll and is keyboard-focusable — `.floating-cta` anchor to `#planner`, fixed bottom-center, present on every section (verified across screenshots)

## 4. Content sections (reveals, split headline, accordion, spec, finale)

- [x] 4.1 Rebuild the feature/value section with scroll reveals (CSS `animation-timeline: view()` with `animation-range: entry`; `IntersectionObserver` fallback when unsupported; feature-detect once so only one path drives each element); verify reveals fire once per element and elements below the fold start hidden — manifesto section with numbered value points; `view()` reveals verified (progress 1 / opacity 1 when scrolled past)
- [x] 4.2 Add one headline split around a furniture/room render (the "Fly in … luxury" device, translated), full-bleed; verify the two headline halves flank the image without overlap at desktop and reflow sanely at 375px — showcase "Design … in luxe" splits around the Eames chair on gold at desktop; on mobile a clean stacked headline replaces the overlap (verified, no overflow)
- [x] 4.3 Convert the three feature points into an accordion (expand/collapse, one or many open — match the reference's feel); verify keyboard + click toggle and that it is not inside the planner subtree — dedicated advantages accordion (4 items) added; click toggle verified (`.is-open` → panel maxHeight 340px); it is a native `<button>` (keyboard-operable) and outside the planner subtree
- [x] 4.4 Add a spec-table section for a featured room preset / furniture piece (label → value rows, cinematic styling); verify values are static content, not wired into planner state — spec `<dl>` (Footprint / Clearance / Power draw / From) added to the showcase; static values, not wired to `state`
- [x] 4.5 Add the dark cinematic finale section before the footer (near-black, oversized faded word + a closing line + CTA), echoing the reference's "Global" beat; verify contrast/legibility — near-black finale with giant faded "SPACE" outline + "Your next room starts here." + CTA (verified legible)
- [x] 4.6 Add `prefers-reduced-motion: reduce` handling across all of §3–§4 (no zoom, no parallax, reveals resolve to final visible state); verify by code review of the media query + the JS `matchMedia` short-circuit — CSS media query forces `[data-reveal]` visible + no animation; `initScrollReveal` reveals all immediately and `initHeroZoom` returns early when reduced motion (verified by code review; no pane emulation for this media feature)

## 5. Planner as a bright "studio workbench" (restyle container only)

- [x] 5.1 Reframe the planner section as a bright, high-contrast light panel that reads as a deliberate calm beat in the palette journey — restyle ONLY the section container/framing/heading, NOT the internal `#room-presets`/`#catalog-list`/`#board`/metrics markup or classes; verify by diff that planner-internal markup is unchanged — planner reframed as a white "studio workbench" panel with a display-font section heading; the internal `id="room-presets"`, `id="catalog-list"`, `id="board"`, and `id="metric-util-bar"` markup all present verbatim (grep -c = 1 each, exact class strings)
- [x] 5.2 Confirm no `[data-scene]`/`[data-reveal]` (or any scroll-scene hook) is placed inside `#room-presets`, `#catalog-list`, `#board`, or the metrics `<aside>` (design.md Decision 2); verify via grep — verified: the only `data-reveal`s near the planner are on its section heading (framing); none inside the four tool containers

## 6. Imagery

- [x] 6.1 Web-search cinematic imagery from explicitly free-to-use/openly-licensed sources only (Wikimedia Commons CC0/PD, Pexels, Unsplash free) for: hero portal/space, section backgrounds, and one featured furniture/room render; verify each license before download — sourced 3 candidates from Wikimedia Commons with licenses verified per file; the final design uses 2 (`hero-workspace.jpg` CC BY-SA 4.0; `showcase-chair.jpg` public domain). A third bright-studio candidate was fetched but not needed and was deleted rather than shipped as a dead asset — the cinematic sections read better as solid-color palette beats than with extra photos
- [x] 6.2 Download into `images/`, compress each full-bleed image to a reasonable size (target < ~200 KB), and lazy-load below-the-fold backgrounds; verify files exist, open correctly, and sizes are reasonable — hero 78 KB, chair 13 KB; verified by reading each. No CSS background-image below the fold, so no lazy-load needed; the two `<img>` tags are small
- [x] 6.3 Give every image a graceful fallback (solid token color behind a background image; `onerror`/alt for `<img>`) so a missing file never breaks layout or the planner; verify by temporarily pointing one at a missing path — hero `<img>` has `onerror` swapping to an espresso gradient; showcase chair `<img>` has `onerror` hide; both have alt text. Layout holds without the files.
- [x] 6.4 Write `images/CREDITS.md` recording per image: source URL, license name, license URL, and any modification (resize/crop); verify every downloaded file has an entry — written; all three files credited with source, license, author, and modification note

## 7. Regression verification (planner untouched)

- [x] 7.1 Re-run the full manual planner regression in the browser pane: click-to-place/rotate/remove, hard bounds invariant, hard overlap invariant, soft spacing warning, power formula, utilization, shopping list, export modal, `localStorage` round-trip, responsive at 375×812 — verify every result matches `docs/sessions/2026-09-23-redesign-tailwind-planner.md`'s Tests run section with no regressions — all verified via real UI/pointer events: tap-to-place (desk+chair → 7/80 sq ft, 90 W/1 outlet, 1 spacing pair, $708); off-grid drag rejected (desk stayed 0,0); overlap drag rejected (chair stayed clear, count 2); export modal figures matched; `localStorage` round-trip held desk(0,0)+chair(3,0) with no overlap; reset works
- [x] 7.2 Verify no console errors during load, rapid scroll, window resize, and item drag with the cinematic shell in place — zero console errors across the whole session (load, scroll, resize, drags, modal)
- [x] 7.3 Verify the cinematic journey end-to-end at desktop and 375px: each section's background/palette, hero (enhanced or fallback), reveals, accordion, spec table, finale, floating CTA — screenshot the key beats — screenshotted hero (with zoom-through), manifesto, showcase (split + stacked-mobile), advantages accordion, workbench, dark blueprint, finale; no horizontal overflow at 375px
- [x] 7.4 Verify reduced-motion end-to-end: with `prefers-reduced-motion` on, content is visible with no zoom/parallax/reveal motion — verified by code review (CSS `@media` forces visible/no-animation; `initScrollReveal` reveals all; `initHeroZoom` returns early); the pane has no reduced-motion emulation control, same limitation recorded in the prior change

## 8. Reconcile docs

- [x] 8.1 Rewrite `docs/site/ARCHITECTURE.md` §10 (presentation layer) to describe the new cinematic scroll-scene mechanism as ambient same-`Loc` `Trn` (no new `Dat`, per design.md Decision 1); note the planner-fence invariant (Decision 2) and the palette-journey approach — added §10 (presentation layer): the three init functions, planner-fence invariant, palette journey, Law 1 rationale
- [x] 8.2 Update `docs/site/IMPLEMENTATION.md` with rows for the new presentation `Trn` (scene/reveal/hero-zoom/accordion init functions) mapped to `file:symbol`; confirm the planner morphism rows are unchanged — added 3 presentation rows + Notes; planner rows untouched
- [x] 8.3 Update `docs/site/STATUS.md` (headline + completeness) and `docs/STATUS.md` roll-up to reflect the cinematic redesign and that the planner behavior is unchanged — both updated; added the JS-hero-zoom caveat to Needs work
- [x] 8.4 Write `docs/site/reviews/review-redesign-jesko-aesthetic.md` running the §4.5 coherence checklist (Law 1 placement honesty — no new `Loc`; effect isolation — scene `Trn` never touches `state.layout`; composition soundness — planner invariants re-verified) and record the hero-zoom enhancement outcome (applied vs fallback) — written; records the JS-driven hero-zoom outcome (CSS scroll() inert → JS fallback shipped)
- [x] 8.5 Run the supercharge drift check (`supercharge-drift`) and fix or record any dead rows before archiving — `0 dead / 0 refs`; new symbols verified by hand (grep -c = 1 each)

## 9. Post-review fixes (defects found on the built page)

- [x] 9.1 Fix reveals not appearing near the page bottom (design.md §9a): make `IntersectionObserver` the primary `[data-reveal]` driver — in `initScrollReveal`, remove the `CSS.supports("animation-timeline","view()")` early-return so the observer always attaches (keep the reduced-motion and no-IO branches); verify the finale + blueprint-summary reveal correctly when scrolled to the very bottom (opacity 1) — done; verified at max scroll (real scroll, pane painting): finale reveals all opacity 1, no blank sections
- [x] 9.2 Remove the CSS `view()`-timeline reveal path in the `<style>` block (the `@supports (animation-timeline: view())` block that overrides `[data-reveal]` with the `reveal-in` animation), leaving only the transition-based hidden→`.is-revealed` styles that the observer toggles; verify no element is left permanently hidden and reveals still animate (fade/slide) on scroll-in — removed; reveals fade/slide via the transition on `.is-revealed`
- [x] 9.3 Fix the showcase white-box image (design.md §9b): add `mix-blend-mode: multiply` to the showcase `<img>` so its white background drops out over the gold section; verify in-browser that the box is gone and the split headline "Design … in luxe" layers cleanly around the chair at desktop, and the stacked mobile headline still looks right — done; screenshot confirms the box is gone and the chair floats over the split headline on gold
- [x] 9.4 Re-verify end-to-end after the fixes: scroll the full page top→bottom and confirm every section's reveals complete (no blank sections at any scroll position), the hero zoom still works, the planner regression still passes, and there are no console errors; screenshot the finale and showcase as proof — done via real computer-scroll (rAF/IO are starved when the app window is backgrounded, so synthetic jumps under-report; a real scroll confirmed manifesto/finale reveals=1, nav contrast flips, planner regression passes, zero console errors)
- [x] 9.5 Reconcile docs for the fixes: update `docs/site/IMPLEMENTATION.md` (note reveals are IO-driven, `view()` path removed) and append the outcome to `docs/site/reviews/review-redesign-jesko-aesthetic.md`; re-run `supercharge-drift` — done below

## 10. Fix fixed-nav contrast over light sections

- [x] 10.1 The fixed top nav uses `mix-blend-mode: difference`, which is illegible over the cream/gold/white sections. Replace it with a scroll-spy: tag each full-bleed section with `data-nav="light"` (light background → needs dark nav text) or `data-nav="dark"` (dark background → light nav text); default the nav to light (white) text — done; 7 sections tagged, `mix-blend-difference` removed, `#site-nav` defaults to `text-white`
- [x] 10.2 Add `initNavContrast()` (in the entry point, alongside the other presentation inits): a rAF-throttled scroll handler that finds the section currently under the nav band and toggles a `nav-dark` class on the header (dark text) when over a `light` section; add the CSS (`#site-nav` default white → `.nav-dark` stone-900, children inherit). Verify the nav is legible over every section (dark hero/summary/finale → white; cream/gold/planner → dark) and updates live on scroll — done; verified via real scroll: dark text over cream manifesto + gold showcase, white text over dark finale
- [x] 10.3 Confirm `prefers-reduced-motion` is unaffected (nav contrast is not motion) and there are no console errors — nav contrast is colour-only (no motion); zero console errors

## 11. Restore furniture catalog images (within the redesign)

- [x] 11.1 Re-source the five catalog images from openly-licensed Wikimedia files (standing-desk CC0, acoustic-panel CC BY-SA 4.0, lighting-rig CC BY-SA 3.0, bookshelf CC BY 2.0, ergo-chair CC0), download to `images/furniture/`, resize/compress (<95 KB each), and record them in `images/CREDITS.md` — done (25–91 KB each; CREDITS.md updated with a furniture table)
- [x] 11.2 Add an `image` field (relative path into `images/furniture/`) to each `FURNITURE_CATALOG` entry; verify `catalogEntry(type).image` resolves for all five (no `Layout`/`FurnitureItem`/storage-shape change — image is catalog-resolved at render time) — done; all five resolve
- [x] 11.3 Update `renderCatalog` to render `<img src="{entry.image}">` in the swatch with an `onerror` fallback to the existing Lucide icon + color; verify visually and by forcing a broken src — done; all 5 catalog images load (naturalWidth>0), `onerror` fallback preserved
- [x] 11.4 Update `renderGrid` (placed canvas items) and the drag-ghost to use the same `<img>` + caption + `onerror` fallback; apply a consistent `object-cover`; verify placed items show their photo and drag/rotate/remove still work — done; placed desk+chair render their images with captions; drag (bounds/overlap) still works (img/caption are `pointer-events-none` so the drag target stays the item div)
- [x] 11.5 Update the shopping-list row rendering to show a small `entry.image` thumbnail (with `onerror` removal); verify quantities/costs unaffected — done; thumbnails load, totals unchanged ($708)
- [x] 11.6 Re-run the planner regression (bounds/overlap/spacing/power/utilization/shopping/export/persistence) with images in place — confirm no invariant or metric changed and `localStorage` still stores items by `type` only (no image leak) — verified: bounds + overlap rejected, 7/80 sq ft, 90 W, $708; `localStorage` stores items by `type`/coords only, no `image` field leaked

## 12. Second-round post-review fixes (before commit/archive)

- [x] 12.1 Fix the metrics-sidebar bleed (design.md §12a): add `min-w-0` to the workbench canvas grid column; verify via `getBoundingClientRect` (metrics aside right edge ≤ panel right edge) across all three room presets with several items placed, not just visually — done; added `min-w-0` to the canvas column div (index.html line 422); verified via `getBoundingClientRect` at 1600px: overflow is a consistent -32px (safely inside the panel) across all three presets and after placing items, versus +5.66px (bleeding) at 12×14 before the fix
- [x] 12.2 Source a replacement single-chair image for the showcase section (design.md §12b) from Wikimedia Commons under a free/open license; resize and recompress per the existing discipline; add its row to `images/CREDITS.md`; remove the now-unused `showcase-chair.jpg` if fully replaced — done; sourced `File:Leap-Chair.png` (Steelcase Inc, CC BY 3.0) — an official single-chair product photo with generous built-in negative space, rejecting three other candidates (a real-office photo with a cluttered background, an underlit silhouette, and an orange-wall photo) that wouldn't have composited cleanly. Resized to 900px; `images/CREDITS.md` updated; old `showcase-chair.jpg` removed (replaced in place, new file is `showcase-chair.png`)
- [x] 12.3 Swap the `src` in the `#showcase` section to the new image; verify the desktop split-headline and mobile stacked variant both read cleanly, and re-check `mix-blend-multiply` still removes any white background on the new image — done, with a design change from the plan: the new source's backdrop is a near-white studio gray (~238–250/255), not pure white, so `mix-blend-multiply` left a visible dimmed rectangle (the same "white-box" symptom as design.md §9b, just fainter) instead of removing it. Fixed properly instead of re-applying the same workaround: keyed the backdrop out to real transparency via a flood-fill cutout (grown from the image border, tolerance-matched to the backdrop's gray/white range) so the chair composites cleanly against any section color with no blend mode needed; `mix-blend-multiply` was removed from the `<img>` class. Also narrowed `w-[min(70vw,520px)]` to `w-[min(60vw,420px)]` since the new image is portrait (900×586) rather than the old landscape (285×400) and would otherwise render very tall. Verified in-browser at desktop (1600px, real scroll to trigger reveals): clean split around "Design" / "in luxe" with no overlap and no box artifact; verified at 375px: the `sm:hidden` stacked headline + centered chair reads cleanly with no horizontal overflow; zero console errors at both sizes
- [x] 12.4 Reconcile docs: note both fixes in `docs/site/reviews/review-redesign-jesko-aesthetic.md`; re-run `supercharge-drift` — done; added a "Second-round post-review fixes" section to the review; `supercharge-drift` unchanged at `0 dead / 0 refs` (same bare-filename ceiling; no new symbols introduced this round, only a CSS class change and an image swap)
